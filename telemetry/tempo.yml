compactor: null
server:
  http_listen_port: 3200
distributor:
    receivers:
      otlp:
        protocols:
          grpc:
          


ingester:
    trace_idle_period: 1s
    lifecycler:
        ring:
            replication_factor: 3
storage:
  trace:
    backend: local
    local:
      path: /var/tempo/traces
    wal:
      path: /var/tempo/wal
